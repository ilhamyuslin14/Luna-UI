--- src/views/kandidat/Kandidat-UnggahCV.jsx
+++ src/views/kandidat/Kandidat-UnggahCV.jsx
@@ -231,6 +231,7 @@
   // Derived state dari Context untuk phase 'uploading'
   const { scoringQueue } = useUpload();
   const ctxFiles = globalFiles;
+  window.scoringQueueDebug = scoringQueue;
   
   const extendedFiles = ctxFiles.map(f => {
     const isUploadAndScoring = !!f.posisi;
     const scoringJob = isUploadAndScoring 
-      ? scoringQueue.find(sq => sq.namaFile === f.name && sq.seleksiId === f.posisi.id && sq.companyId === f.companyId)
+      ? scoringQueue.find(sq => {
+          const match = sq.namaFile === f.name && sq.seleksiId === f.posisi.id && sq.companyId === f.companyId;
+          if (!match && sq.namaFile === f.name) console.log("Mismatched scoring job!", sq, f);
+          return sq.namaFile === f.name;
+        })
       : null;
 
     let finalStatus = 'waiting';
